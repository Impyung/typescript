function App() {
    let array = [];
    const obj = {};
    let name1 = 123;
    function times(x) {
        return x * 2;
    } // 숫자 들어와야하고 숫자 리턴해야함
    // type Member = {
    //   [key : string] : string;
    // }
    let john = { name: 'kim' };
    class User {
        name;
        constructor(name) {
            this.name = name;
        }
    }
    console.log(times(1));
}
